[project]
name = "github-orm"
version = "0.1.0"
description = ""
authors = [
    {name = "Daniel Ortiz Pacheco",email = "danielortizpacheco@example.com"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "requests (>=2.32.5,<3.0.0)",
    "python-dotenv (>=1.2.1,<2.0.0)",
    "pytest (>=9.0.1,<10.0.0)",
    "pytest-cov (>=6.0.0,<7.0.0)",
    "dotmap (>=1.3.30,<2.0.0)"
]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
